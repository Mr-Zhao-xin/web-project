<template>
    <div>
        用户管理
    </div>
</template>
<script setup lang="ts">
/*
      <el-sub-menu v-for="(item, index)  in visibleMenuList" :key="item.path" :index="`${item.path}`">
        <template #title>
          <el-icon>
            <location />
          </el-icon>
          <!-- 判断是否有meta属性和item.meta.title -->
          <span v-if="item.meta && item.meta.title">{{ item.meta.title }}</span>
        </template>
        <!-- 循环visibleMenuList下边的children路由嵌套 -->
        <el-menu-item-group v-for="(items, index) in item.children" :key="index">
          <el-menu-item :index="`/` + items.path">
            <!-- 继续判断children里边有没有items.meta && items.meta.title -->
            <span style="margin-left: 20px" v-if="items.meta && items.meta.title">{{ items.meta.title }}</span>
          </el-menu-item>

        </el-menu-item-group>
      </el-sub-menu>
*/
</script>